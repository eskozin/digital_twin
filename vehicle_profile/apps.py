from django.apps import AppConfig
from django.contrib import staticfiles


class VehicleProfileConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'vehicle_profile'
